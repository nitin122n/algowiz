/** Backtracking, number theory, and data-structure classics. */
import { hanoi, nQueens, permutations, subsetSum, subsets, sudoku } from './backtracking';
import { fastPower, gcd, sieve } from './numeric';
import { deque, queue, stack } from './structures';

/** Classics in sidebar order. */
export const CLASSICS = [nQueens, sudoku, hanoi, permutations, subsets, subsetSum, sieve, gcd, fastPower, stack, queue, deque];
